import { style } from '@vanilla-extract/css';

export const pageHeader = style({
  flexShrink: 0,
  marginBottom: 8,
});

export const pageHeaderDesc = style({
  margin: 0,
  fontSize: 13,
  color: 'var(--color-text-muted)',
});
